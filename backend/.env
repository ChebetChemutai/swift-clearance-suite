# Database Configuration
POSTGRES_DB=holivet
POSTGRES_USER=holivet_user
POSTGRES_PASSWORD=strongpassword
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Backward-compatible DB_* variables
DB_HOST=localhost
DB_PORT=5432
DB_USER=holivet_user
DB_PASSWORD=strongpassword
DB_NAME=holivet

# Standard Connection URLs (Choose the one your framework uses)
# For Node.js (pg), Python (Psycopg2), etc.
DATABASE_URL=postgresql://holivet_user:strongpassword@localhost:5432/holivet
